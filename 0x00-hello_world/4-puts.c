#include <stdio.h>
/**
 * main - Entry point for the program
 *
 * Return: 0 on success, non-zero value on failure
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	/**
	*  return main is 0 (success)
	*/
	return (0);
}
