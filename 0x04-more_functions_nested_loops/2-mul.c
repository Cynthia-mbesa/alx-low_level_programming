#include "main.h"
/**
 *mul - multiplies two integers
 *@a: the first integer in the multiplication
 *@b: the second integer in the multiplication
 *Return: gives the answer of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
